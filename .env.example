# Example environment variables (commit this file)
# Replace values in your local .env (do NOT commit .env)

# Server-only keys (do not expose to client)
MY_SERVICE_API_KEY=replace-with-your-gemini-api-key
GOOGLE_TTS_API_KEY=replace-with-your-google-cloud-tts-api-key

# Public env vars (client-safe)
PUBLIC_FIREBASE_API_KEY=replace-with-your-firebase-api-key
