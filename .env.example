# Example environment variables (commit this file)
# Replace values in your local .env (do NOT commit .env)

# Example public env var (Astro will expose PUBLIC_ prefixed to client)
# Only add values safe to expose
PUBLIC_GEMINI_API_KEY=replace-with-your-api-key
PUBLIC_FIREBASE_API_KEY=replace-with-your-api-key
