---
// Improvement idea: get actual width as props, and auto-set height by correct aspect-ratio
const { width = 122, height = 50, alt = 'IdeasKidsCan logo', class: className = '' } = Astro.props;
---

<img src="/images/IdeasKidsCanLogo.png" alt={alt} width={width} height={height} class={className} style="display:block;" />
